{
  "name": "spa-m3-prueba-desmpe-o",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "backend": "json-server --watch public/db.json --port 3000"

  },
  "devDependencies": {
    "vite": "^7.0.4"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
